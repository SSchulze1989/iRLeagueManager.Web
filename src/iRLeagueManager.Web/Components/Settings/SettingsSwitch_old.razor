@* <MudListItem
    Icon="@Icon"
    IconSize="IconSize"
    IconColor="IconColor">
    <MudGrid>
        <MudItem xs="10">
            <MudField Variant="Variant.Text" HelperText="HelperText">
                @if (ChildContent is not null)
                {
                    @ChildContent
                }
                else
                {
                    @Text
                }
            </MudField>
        </MudItem>
        <MudItem xs="2"> *@
            @* <MudSwitch T="bool" Checked="Test" CheckedChanged="TestChanged" Color="Color" /> *@
    @*     </MudItem>
    </MudGrid>
</MudListItem> *@

@code {
    // [Parameter] public string Text { get; set; } = string.Empty;
    // [Parameter] public string HelperText { get; set; } = string.Empty;
    // [Parameter] public Color Color { get; set; } = Color.Inherit;
    // [Parameter] public string Icon { get; set; } = string.Empty;
    // [Parameter] public Size IconSize { get; set; } = Size.Medium;
    // [Parameter] public Color IconColor { get; set; } = Color.Inherit;
    // [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool Expanded { get; set; } = false;
    [Parameter] public EventCallback<bool> ExpandedChanged { get; set; }
}
